# tunnel-client local runtime

Place the OpenAI `tunnel-client.exe` downloaded from the Platform tunnel settings or the
official `openai/tunnel-client` release in this directory. The executable is local runtime
state and is ignored by Git; only this installation note is source-controlled.
